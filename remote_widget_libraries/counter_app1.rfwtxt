import core.widgets;
import core.material;

widget Counter = Scaffold(
                       backgroundColor:0xFF66AACC,
                       body: Column(
                         children: [SizedBox(height: 50), Center(child: Text(text:"data"))],
                       ),
                     );
