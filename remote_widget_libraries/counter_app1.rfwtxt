import core.widgets;
import core.material;

widget Counter = Scaffold(
                       backgroundColor:0xFF65ABCC,
                       body: Column(
                         children: [SizedBox(height: 500.00), Center(child: Text(text:"Vikasfggd"))],
                       ),
                     );
